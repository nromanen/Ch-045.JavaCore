package net.students.data;

/**
 * Base Columns interface
 * Created by Igor Gavryliuk on 26.10.2016.
 */
public interface BaseColumns {

    //The unique ID for a row. INTEGER (long)
     String ID = "id";

    //The count of rows in a directory. INTEGER
     String COUNT = "count";
}
